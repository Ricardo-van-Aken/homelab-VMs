---
# One group per service. A VM joins every group whose service it hosts.
all:
  children:
    jellyfin:
      hosts:
        arcade:
    gaming:
      hosts:
        arcade:
    forgejo:
      hosts:
        forge:
