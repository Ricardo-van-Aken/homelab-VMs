---
# Inherits .config/molecule/config.yml. Docker runs inside the instance, so
# the compose stack really starts.
scenario:
  name: forgejo
provisioner:
  inventory:
    group_vars:
      all:
        forgejo_domain: localhost
        forgejo_admin_password: molecule-test-only
