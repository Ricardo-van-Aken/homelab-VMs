---
# Two kinds of machines: the Proxmox host(s) and the VMs on them. VMs carry
# one group per service and join every group whose service they host.
all:
  children:
    proxmox:
      hosts:
        srv1:
    vms:
      children:
        jellyfin:
          hosts:
            arcade:
        gaming:
          hosts:
            arcade:
        forgejo:
          hosts:
            forgejo-git:
